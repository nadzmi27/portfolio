---
import HeaderClient from "./HeaderClient";
const base = import.meta.env.BASE_URL;
const segments = Astro.url.pathname.split("/").filter(Boolean);
---

<HeaderClient client:load segments={segments} base={base}/>
